// TODO: Add MSW (Mock Service Worker) handlers here if API mocking is introduced
// Example: http.get('/api/content/:section', ({ params }) => HttpResponse.json(...))

export const handlers: unknown[] = [];
